apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-managed-disk-backendpvc
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-premium-retain-sc
  resources:
    requests:
      storage: 5Gi
